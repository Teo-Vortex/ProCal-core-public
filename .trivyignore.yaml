misconfigurations:
  - id: AVD-DS-0002
    paths:
      - Dockerfile
      - updater/Dockerfile
    statement: The Core entrypoint drops to UID/GID 1000 after fixing volume ownership. The isolated updater requires root access to the Docker socket, has no host port, and receives no application credentials or data volumes.
