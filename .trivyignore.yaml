misconfigurations:
  - id: AVD-DS-0002
    paths:
      - Dockerfile
    statement: The root entrypoint only fixes persistent-volume ownership, then uses setpriv to run the Node process as UID/GID 1000.
